

const PageTitle = ({title} : {title: string}) => {
  return (
    <h1 className="text-2xl !font-semibold uppercase text-[#222831] ">
        {title}    
    </h1>
  )
}

export default PageTitle
